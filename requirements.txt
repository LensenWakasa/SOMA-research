# acsis/requirements.txt
# ─────────────────────────────────────────────────────────
# Core
aiohttp>=3.9.0
nest-asyncio>=1.6.0

# ML (Phase 2+)
torch>=2.0.0
transformers>=4.40.0
peft>=0.10.0
bitsandbytes>=0.43.0      # 4-bit quantization

# Memory
chromadb>=0.4.0
sentence-transformers>=2.7.0

# Knowledge graph (optional — falls back to in-memory)
neo4j>=5.0.0

# Tools
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# Search (optional — falls back to DuckDuckGo)
tavily-python>=0.3.0

# Notifications (optional)
python-telegram-bot>=20.0

# Dev
pytest>=7.0.0
pytest-asyncio>=0.23.0
